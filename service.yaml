apiVersion: v1
kind: Service

metadata:
  name: kafdrop
  namespace: kafka-tools

  labels:
    app: kafdrop
    environment: production

spec:
  type: ClusterIP

  selector:
    app: kafdrop

  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP

